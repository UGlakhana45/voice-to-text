# React Native + Expo expect a flat node_modules layout for autolinking
# (gradle scripts use require.resolve from android/, podspecs traverse
# node_modules/, etc.). pnpm's default isolation breaks both.
node-linker=hoisted
shamefully-hoist=true
